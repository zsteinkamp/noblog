- if entry.thumb_url
  .image_wrapper
    = link_to image_tag(entry.thumb_url), entry.permalink
%h2= link_to entry.data[:title], entry.permalink
= entry.data[:excerpt].blank? ? "#{strip_tags(entry.data[:content])[0..255]}..." : entry.data[:excerpt]

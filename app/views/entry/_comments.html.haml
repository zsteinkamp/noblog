%a{:name => "comments"}
- if @entry.comments.count > 0
  %h3.comment-header Comments
  %ul#comments
    - @entry.comments.each do |comment|
      %li
        .whowhen
          = comment.data[:author]
          on #{comment.created_at.strftime("%b %d, %Y")} wrote...
        = comment.data[:comment].gsub(/\n\s*\n/, "<br/><br/>")
